# CutImage 项目总览与 AI 交接文档

## 1. 项目目标

本项目是一个基于 `Tkinter + OpenCV + Pillow + NumPy` 的本地桌面工具，用于批量识别、裁剪并拼接一组比色皿图片。

当前工具的核心目标包括：

1. 自动识别原图中的有效主体区域。
2. 支持人工微调单张图片的裁剪框。
3. 生成带统一样式的最终拼接图。
4. 支持保存项目并在之后恢复继续编辑。
5. 支持中文路径、中文文件名和中文项目文件。
6. 提供桌面 GUI，而不是只依赖脚本命令行。

## 2. 当前运行方式

### 2.1 Python 环境

项目约定使用本机 Miniconda 中的 `cutimage` 环境运行。

当前启动入口：

1. `run_cutimage.bat`
2. `app.py`

推荐直接双击：

1. `run_cutimage.bat`

### 2.2 程序入口

主入口文件：

1. `app.py`

核心桌面界面逻辑：

1. `ui/main_window.py`

## 3. 当前目录结构说明

### 3.1 核心代码

1. `app.py`
   程序入口。
2. `core/models.py`
   项目数据模型、处理设置、裁剪框结构。
3. `core/image_processor.py`
   图片读取、自动识别、裁剪、样式处理、拼接、保存。
4. `core/project_store.py`
   项目文件保存与读取。
5. `ui/main_window.py`
   Tkinter 界面、批量处理、预览、导出、项目恢复逻辑。

### 3.2 文档

1. `最初指导文档.txt`
   初始需求来源。
2. `docs/superpowers/specs/2026-05-12-cutimage-desktop-tool-design.md`
   第一版桌面工具设计文档。
3. `docs/superpowers/specs/2026-05-12-cutimage-rounded-corners-design.md`
   圆角矩形导出设计文档。
4. `docs/superpowers/specs/2026-05-18-cutimage-final-export-resolution-design.md`
   最终拼接图导出分辨率设计文档。
5. `docs/superpowers/specs/2026-05-18-cutimage-stretch-align-design.md`
   新增上下对齐（拉伸）模式设计文档。
6. `docs/superpowers/specs/2026-05-18-cutimage-stretch-height-control-design.md`
   拉伸统一高度独立控制设计文档。
7. `docs/superpowers/specs/2026-05-19-cutimage-stretch-average-height-design.md`
   拉伸统一高度自动取平均高度设计文档。
8. `docs/superpowers/specs/2026-05-19-cutimage-final-export-name-design.md`
   最终导出名称可配置设计文档。

### 3.3 测试与中间目录

以下目录主要是开发过程中保留下来的验证目录：

1. `_smoke`
2. `_style_check`
3. `_rounded_check`
4. `_rounded_export_test`
5. `_export_reuse_test`
6. `_incremental_export_debug`
7. `_incremental_export_test`
8. `_incremental_export_test2`
9. `_incremental_export_test3`
10. `_validation_dual_export`
11. `中文测试`

这些目录可以继续作为功能验证参考，也可以在确认不再需要后再统一清理。

## 4. 当前已实现的功能

### 4.1 项目管理

1. 新建项目。
2. 打开已有项目。
3. 保存项目。
4. 另存项目。
5. 保存后重新打开可恢复图片顺序、裁剪框、样式参数、导出目录与最终导出名称配置。

### 4.2 图片处理

1. 批量读取源目录中的图片文件。
2. 自动识别主体区域。
3. 支持人工拖拽裁剪框修正。
4. 支持手动输入上下左右边界数值修正。
5. 支持按文件名自动排序。
6. 支持手动上移、下移图片顺序。

### 4.3 结果样式

1. 背景支持：
   `纯黑` / `纯白`
2. 对齐方式支持：
   `顶部对齐` / `居中对齐` / `底部对齐` / `上下对齐（拉伸）`
3. 白底时支持可选细灰边线。
4. 单张裁剪图支持：
   `直角矩形` / `圆角矩形`
5. 最终拼接图支持：
   `直角矩形` / `圆角矩形`
6. 单张裁剪图和最终拼接图的圆角半径可独立控制。
7. 拉伸模式支持独立参数 `拉伸统一高度`。

### 4.4 导出

1. 单张裁剪图导出到：
   `cropped/`
2. 最终拼接图导出到：
   `final/<最终导出名>.png`
3. 项目副本导出到：
   `project/`
4. 导出目录会保存在项目中，后续再次导出时直接复用。
5. 最终拼接图名称支持在 GUI 中配置，并支持默认跟随项目名。
6. 最终拼接图采用增量命名，不覆盖旧图。
7. 当前最终拼接图只导出一张，不再额外生成缩略版双产物。
8. 当前导出链路只允许无损 `png`。

## 5. 当前重要行为规则

### 5.1 中文路径支持

已经专门处理并验证以下场景：

1. 中文目录路径。
2. 中文图片文件名。
3. 中文项目文件路径。

实现方式：

1. `core/image_processor.py` 中的 `read_image()` 不使用 `cv2.imread`。
2. 改为使用 `np.fromfile + cv2.imdecode`。

这样是为了避免 Windows 下 OpenCV 读取中文路径不稳定的问题。

### 5.2 拼接图外边距

最终拼接图不仅保留图片之间的间距，还会在四周自动留白。

这样可以避免右侧最后一张图片贴边时视觉不完整的问题。

### 5.3 导出目录复用

如果项目文件中已经保存了 `output_dir`：

1. 再次点击“导出结果”时，不再重新询问导出目录。
2. 会继续写入原来的导出目录。

### 5.4 最终拼接图增量导出

当前最终拼接图不会覆盖旧文件。

当前基础名称来源规则为：

1. 默认勾选“与项目名一致”时，基础名称取项目名。
2. 取消勾选并填写“最终导出名”时，基础名称取自定义名称。
3. 取消勾选但留空时，基础名称仍回退为项目名。

示例：

1. 第一次导出：
   `final/项目名.png`
2. 第二次导出：
   `final/项目名_001.png`
3. 第三次导出：
   `final/项目名_002.png`

### 5.5 最终拼接图尺寸策略

当前最终拼接图只保留一条主导出链路：

1. `final/<最终导出名>.png`
   作为默认最终结果图命名模板。

普通三种对齐模式下：

1. 使用原始裁剪尺寸直接拼接。
2. 不再额外生成按 `target_height` 缩放的附带缩略图。

拉伸模式下：

1. 所有单图统一按 `stretch_target_height` 做纵向拉伸或压缩后再拼接。
2. 因为当前 `stretch_target_height` 会自动更新为平均高度，所以拉伸模式下导出的最终图会直接反映当前平均高度策略。

### 5.6 平均高度策略

当前 `stretch_target_height` 已不再主要依赖固定默认值，而是会在裁剪结果变化后自动回到当前有效裁剪图的平均高度。

触发更新的场景包括：

1. 批量处理完成后。
2. 手动输入边界修正后。
3. 拖拽裁剪框修正后。
4. 清除手动修正并重新处理后。

平均高度采用四舍五入后的整数。

### 5.7 打开项目后的预览恢复

当前打开已保存项目时：

1. 不需要再手动点击一次“批量处理”。
2. 程序会根据项目文件中保存的裁剪框，重新读取原图并恢复 `processed_crops`。
3. 拼接预览会在打开项目后直接恢复。

实现方式不是额外保存一份预览图文件，而是：

1. 保存裁剪框。
2. 打开项目时按裁剪框重新构建缓存。

这样可以避免保存冗余预览文件，也能避免预览图过期。

### 5.8 导出格式限制

当前界面与底层保存逻辑都只允许无损 `png`：

1. GUI 中不再提供 `jpg`、`jpeg` 导出入口。
2. 项目配置里的 `output_format` 读取后会自动规范为 `png`。
3. 底层保存函数若收到非 `.png` 路径会直接拒绝。

### 5.9 单张裁剪图导出策略

当前 `cropped/` 目录中的单张裁剪图仍按原文件名输出。

因此同一项目重复导出时：

1. 单张裁剪图会被后一次结果覆盖。
2. 这不是当前 bug，而是保留行为。

如果后续需要，也可以再改成和 `final/` 一样的增量模式。

### 5.10 批量处理异步执行

当前点击“批量处理”后：

1. 实际图片处理在后台线程执行。
2. GUI 主线程只负责刷新进度条、进度文本和最终界面收尾。
3. 处理过程中界面不会再表现得像卡死。

当前界面行为：

1. 顶部显示进度条。
2. 显示类似“正在处理 12 / 57：xxx.png”的状态文本。
3. 处理中会临时禁用新建项目、打开项目、保存项目、另存项目、批量处理、导出结果等工具栏按钮。
4. 处理中会阻止拖拽裁剪框、手动修正、清除修正、调整顺序等会冲突的操作。

### 5.11 导出前自动触发处理

如果用户在当前项目中还没有有效处理缓存就直接点击“导出结果”：

1. 程序会先自动启动一轮异步批量处理。
2. 批量处理完成后再自动继续导出。
3. 如果当前已经在处理，则会提示先等待处理完成。

### 5.12 最终导出名称配置

当前顶部工具栏支持单独配置最终拼接图导出名称：

1. 新增“最终导出名”输入框。
2. 新增“与项目名一致”勾选框。
3. 默认勾选“与项目名一致”。
4. 勾选时，“最终导出名”输入框变灰且不可编辑。
5. 取消勾选后，输入框恢复可编辑。
6. 自定义名称和勾选状态会随项目一起保存与恢复。
7. 导出前会对名称做基础清洗，避免 Windows 非法文件名字符导致保存失败。

## 6. 当前 GUI 主要界面结构

### 6.1 左侧

图片列表区域：

1. 显示缩略图。
2. 显示文件名与状态。
3. 可选择当前编辑图片。
4. 支持上移、下移。
5. 支持恢复自动排序。

### 6.2 中间

预览区域：

1. 原图与裁剪框。
2. 当前裁剪结果预览。
3. 最终拼接图预览。

### 6.3 右侧

参数与日志区域：

1. 全局参数。
2. 单张裁剪图样式。
3. 最终拼接图样式。
4. 单图修正。
5. 日志输出。

### 6.4 顶部

工具栏与处理状态：

1. 新建项目。
2. 打开项目。
3. 保存项目。
4. 另存项目。
5. 批量处理。
6. 导出结果。
7. 项目名输入框。
8. 最终导出名输入框。
9. “与项目名一致”勾选框。
10. 批量处理进度文本。
11. 批量处理进度条。

## 7. 当前核心实现说明

### 7.1 数据模型

位于：

1. `core/models.py`

主要结构：

1. `Rect`
   表示裁剪框。
2. `ProcessingSettings`
   表示处理参数和样式参数。
3. `ImageState`
   表示单张图片的识别结果、人工修正结果和状态。
4. `ProjectData`
   表示整个项目，并包含最终导出名称与是否跟随项目名的配置。

当前默认值已调整为：

1. 背景默认 `纯白`
2. 对齐默认 `上下对齐（拉伸）`
3. 单张裁剪图默认 `圆角矩形`
4. 最终拼接图默认 `圆角矩形`

### 7.2 图片处理逻辑

位于：

1. `core/image_processor.py`

主要函数：

1. `read_image()`
   兼容中文路径读取图片。
2. `detect_crop_rect()`
   自动识别裁剪框。
3. `crop_image()`
   按矩形裁剪。
4. `apply_shape_style()`
   应用直角或圆角样式。
5. `build_final_image()`
   生成最终拼接图。
6. `save_rgb_image()`
   统一以无损 `png` 保存。

### 7.3 项目保存逻辑

位于：

1. `core/project_store.py`

说明：

1. 项目文件使用 JSON 保存。
2. `source_dir` 与 `output_dir` 尽量写入相对路径。
3. 还会保存 `final_image_name` 与 `final_image_name_sync_with_project`。
4. 打开项目时会恢复参数、图片顺序、裁剪框、最终导出名称配置等信息。
5. 打开项目后会再根据保存的裁剪框恢复拼接缓存与拼接预览。

### 7.4 GUI 主逻辑

位于：

1. `ui/main_window.py`

负责：

1. 构建桌面界面。
2. 管理图片列表。
3. 响应批量处理。
4. 管理项目名与最终导出名配置。
5. 响应导出。
6. 处理单图拖拽修正。
7. 刷新预览。
8. 恢复项目缓存。
9. 管理异步批量处理进度。

## 8. 从最初需求到当前版本的阶段性演进

### 阶段 1：读取初始需求

来源：

1. `最初指导文档.txt`

核心诉求：

1. 批量识别并裁剪图片主体。
2. 将结果整齐拼接。
3. 后续可以重复使用，不想每次手工改脚本。

### 阶段 2：确定产品形态

明确为：

1. 不做纯命令行。
2. 做桌面 GUI 工具。
3. 需要项目保存与恢复。
4. 需要人工修正能力。

### 阶段 3：完成桌面工具第一版

落地文件：

1. `app.py`
2. `core/models.py`
3. `core/image_processor.py`
4. `core/project_store.py`
5. `ui/main_window.py`
6. `run_cutimage.bat`

### 阶段 4：补齐样式能力

逐步增加：

1. 黑白背景。
2. 对齐方式切换。
3. 白底细灰边线。
4. 单张裁剪图与最终拼接图圆角控制。

### 阶段 5：补齐导出与项目复用

逐步修复：

1. 导出目录复用。
2. 最终图增量命名。
3. 只允许无损 `png`。

### 阶段 6：新增拉伸模式

新增：

1. `上下对齐（拉伸）`
2. 独立参数 `拉伸统一高度`
3. 拉伸模式下上下齐平

### 阶段 7：拉伸高度自动平均

调整：

1. `stretch_target_height` 自动根据当前有效裁剪图平均高度更新。
2. 减少拉伸模式下过度压扁或拉长的情况。

### 阶段 8：最终图从双产物改为单产物

现状：

1. 不再同时导出 `combined_result_target_height.png`
2. 只保留 `final/<最终导出名>.png`

### 阶段 9：默认设置更新

新建项目默认值已改为：

1. `纯白` 背景
2. `上下对齐（拉伸）`
3. `圆角矩形` 裁剪图
4. `圆角矩形` 最终拼接图

### 阶段 10：打开项目后自动恢复拼接预览

改进：

1. 打开项目后自动重建 `processed_crops`
2. 不再需要再点一次“批量处理”才能看到拼接预览

### 阶段 11：GUI 文案清理

改进：

1. 清理了 `ui/main_window.py` 中用户可见的历史乱码文案。
2. 工具栏、面板标题、弹窗、日志、状态文案已统一恢复为正常中文。

### 阶段 12：批量处理异步化与进度条

改进：

1. 批量处理改为后台线程执行。
2. 顶部新增进度文本和进度条。
3. 界面不再像卡死。
4. 导出前缺少处理缓存时可自动先处理再导出。

### 阶段 13：最终导出名称可配置

改进：

1. 顶部新增“最终导出名”输入框和“与项目名一致”勾选框。
2. 默认使用项目名作为最终拼接图导出名称。
3. 取消勾选后可手动设置最终拼接图文件名。
4. 自定义导出名称和勾选状态可保存与恢复。
5. 导出前会清洗非法文件名字符。

## 9. 已明确验证过的能力

### 9.1 基础处理

已验证：

1. 自动识别。
2. 裁剪。
3. 拼接。
4. 项目保存与读取。

### 9.2 中文路径

已验证：

1. 中文目录。
2. 中文图片文件名。
3. 中文项目文件。

### 9.3 圆角输出

已验证：

1. 单张裁剪图圆角。
2. 最终拼接图圆角。
3. 圆角参数可保存与恢复。

### 9.4 导出行为

已验证：

1. 导出目录自动复用。
2. 最终拼接图增量命名。
3. 最终导出图名称可跟随项目名或自定义。
4. 自定义导出名称可保存与恢复。
5. 只允许无损 `png`。
6. 当前只导出一张最终拼接图。

### 9.5 拉伸模式

已验证：

1. 拉伸模式下统一使用 `stretch_target_height`。
2. `stretch_target_height` 会自动更新为平均高度。
3. 打开项目后可恢复拉伸模式相关设置与预览。

### 9.6 异步批量处理

已验证：

1. 后台线程批量处理。
2. GUI 进度条刷新。
3. 处理中禁用冲突操作。
4. 语法检查通过：
   `python -m py_compile app.py core/models.py core/image_processor.py core/project_store.py ui/main_window.py`

## 10. 当前已知限制与后续可优化点

1. `cropped/` 中的单张裁剪图仍会按文件名覆盖。
2. 左侧图片列表还不支持直接拖拽排序。
3. 当前只做了“异步处理 + 进度条”，还没有“取消处理”按钮。
4. 拼接预览本质上仍是预览图，不是最终图的 1:1 显示。
5. 批量处理线程目前只负责执行，不支持中途暂停或取消。
6. 如果后续需要“锁定拉伸高度，不再自动回到平均值”，还需要新增开关。

## 11. 如果后续交接给新的 AI，建议直接说明这些重点

1. 这是一个 `Tkinter + OpenCV` 的本地桌面工具。
2. 运行入口是 `app.py`，推荐启动 `run_cutimage.bat`。
3. 核心文件是：
   `core/models.py`
   `core/image_processor.py`
   `core/project_store.py`
   `ui/main_window.py`
4. 项目支持中文路径读取。
5. 最终拼接图现在只导出一张：
   `final/<最终导出名>.png`
6. 默认勾选“与项目名一致”，也可以取消勾选后自定义最终导出名。
7. 新建项目默认设置是：
   白底、上下对齐（拉伸）、圆角矩形。
8. `stretch_target_height` 会在裁剪结果变化后自动更新为平均高度。
9. 打开项目后会根据保存的裁剪框自动恢复拼接缓存和拼接预览。
10. 批量处理已经异步化，带进度条，处理中会禁用冲突操作。
11. 导出前如果当前没有处理缓存，程序会自动先处理再导出。

## 12. 最简交接结论

如果只想让新的 AI 快速理解项目，可以概括为：

这是一个使用 `Tkinter + OpenCV + Pillow + NumPy` 实现的本地桌面批处理工具，用于自动识别和裁剪比色皿图片，并生成可配置样式的最终拼接图。当前版本已经支持项目保存与恢复、中文路径读取、单图人工修正、黑白背景、四种对齐方式、圆角矩形、平均高度驱动的拉伸模式、单产物最终图导出、导出目录复用、最终导出名称跟随项目名或自定义、增量命名、只允许无损 `png` 导出，以及打开项目后自动恢复拼接预览。最近又新增了批量处理后台线程与进度条，使界面在处理大量图片时不再像卡死，同时也支持导出前自动先完成批量处理。
